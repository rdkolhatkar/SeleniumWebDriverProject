serenity {
  project.name = "Selenium BDD Login Automation"
  reports.show.step.details = true
  reports.take.screenshots = FOR_EACH_ACTION  // Capture screenshots at every action step
}

serenity {
    outputDirectory = "target/site/serenity"
}
